After discussion with the user, the incident will be closed and followed up through a ServiceNow request. During our checks, we created a table, a view, and a materialized view in a catalog schema. When listing the schema content from Spark, only the table was visible, while the view and the materialized view were not returned. As the resolution requires further investigation and handling through a formal request, a ServiceNow request has been created and the follow-up will continue there: <link to the request>.
